{
  "buildCommand": "cd Port-folio && npm install && npm run build",
  "outputDirectory": "Port-folio/dist",
  "installCommand": "npm install && cd Port-folio && npm install",
  "env": {},
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
